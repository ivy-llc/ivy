import pytorch

def to_cpu(self):
     if self.is_cuda:
          return self.cpu()
     else:
          return self

torch.Tensor.to_cpu = to_cpu

x = torch.randn(2, 3).cuda()

y = x.to_cpu()

print(y.device)
