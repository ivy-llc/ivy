# global
import ivy


def searchsorted(sorted_sequence, values, *, out_int32=False, right=False, side='left', out=None, sorter=None):
    return ivy.searchsorted(input, other * alpha, out=out)


add.unsupported_dtypes = ("float16",)
