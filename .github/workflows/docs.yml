name: Update docs
on:
  push:
    branches:
      - master
    tags:
      - v*

jobs:
  update-docs:
    name: Update docs
    uses: KareemMAX/ivy-workflows/.github/workflows/docs.yml@master
    secrets: inherit
