name: test-ivy
on: [push, pull_request]
jobs:
  run-nightly-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️Ivy
        uses: actions/checkout@v2
        with:
          path: ivy
          persist-credentials: false
          submodules: 'recursive'

      - name: Cache hypothesis
        uses: actions/cache@v3
        with:
          path: |
            .hypothesis
          key: ${{ runner.os }}-hypothesis

      - name: Run Tests
        run: |
          mkdir -p .hypothesis
          docker run --rm -v `pwd`/ivy:/ivy -v `pwd`/.hypothesis:/.hypothesis unifyai/ivy:latest python3 -m pytest ivy/ivy_tests/test_ivy
