name: Pre-Release Workflow
on:
  workflow_dispatch:

jobs:
  integration-tests:
    name: Run Integration Tests
    uses: ivy-llc/ivy-integration-tests/.github/workflows/qa_integration_tests.yml@main
    with:
      binaries-branch: nightly
    secrets: inherit
